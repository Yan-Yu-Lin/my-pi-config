# Project instructions

This repo contains personal Pi extensions/configuration.

- Keep extensions small and composable.
- Prefer stable public Pi extension APIs over private internals.
- Run `pnpm check` after TypeScript changes when dependencies are installed.
- Do not add destructive automation without an explicit stop/confirmation path.
